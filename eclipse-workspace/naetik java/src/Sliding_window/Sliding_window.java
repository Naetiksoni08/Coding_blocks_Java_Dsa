package Sliding_window;

public class Sliding_window {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		//it is a algorithm
		// sliding window is of two types fix size and variable size window
		//window means sub array size 
		//fix size window means fix size like k = 3 when the window size is fixed 
		// the first step to solve fix size window is you should know the sum of the first window we will take k elements sum 
		// 0 to k ka sum 
		//  we will take a sum variable 
		// sum mai store karlengew then window ko grow karayenge 
		// variable window steps grow shrink and count the answer
		
		
// fixed size if we are asked to make a sub array whose length is dependent upon k mtlb ki agar hume ye bola hai ki aap ko k jitni length ki maximum sub array bana na h then that is fixed size
		
// whereas variable size mai apko maximum bana ni h voh kitni bhi size ki ho we do not care so
		
		
		
		
	}

}
